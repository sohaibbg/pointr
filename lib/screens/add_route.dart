import 'package:flutter/material.dart';

class AddRoute extends StatelessWidget {
  const AddRoute({required this.name, super.key});
  final String name;
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
